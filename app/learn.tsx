// Given an array Arrof size N, print second largest
// distinct element from an array
let result = [12, 15, 1, 10, 34, 1]
let result2 = [10,5,10]
 const check = ()=> {
    let res = result.findIndex((item)=> result.length >= 34)
    console.log(res)
 }